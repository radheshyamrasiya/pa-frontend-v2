export class CapturePostQueryModel {
        legalName: string;
        smsPhone: string;
        capturedBy: number;
        area: string;
        address: string; //TODO: change to pin once the backend support is there
}